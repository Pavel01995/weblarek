https://github.com/Pavel01995/weblarek
# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

## Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные (Model)

В приложении используется две сущности, описывающие данные: товар и покупатель.

### Интерфейс товара:

Описывает струтуру товара отдельного товара, представленого в магазине.

```typescript
interface IProduct{
id:string;
image:string;
category:string;
price:number|null;
description:string;
}
```

### Интерфейс покупателя:

Описывает данные покупателя, которые он должен указать при офомлении товара.

```typescript
interface IBuyer{
payment:TPayment;
adress:string;
phone:string;
email:string;
}
```


### Класс BasketData
**Назначение:** отвечает за хранение товаров, которые пользователь выбрал для покупки, и за проведение расчетов (сумма, количество).

**Конструктор:**
`constructor()`- конструктор не принимает параметров, при создании объекта инициализируется пустой массив товаров.


**Поля класса:**
`items: IProduct[]` — массив товаров, добавленных в корзину.

**Методы класса:**
`addItem(product:IProduct):void`- добавить товар в корзину.
`removeItem(id:string):void`-удалить товар из корзины.
`getAmount():number`- показывает общее количество товаров в корзине(количество).
`getItems():Iproduct[]`- показывает  массив товаров в корзине(список)
`getTotalPrice():number`- рассчитывет общую стоимость товаров в корзине.
`hasItem(id:string):boolean`- проверяет наличие товара в корзине по id.
`clearItems():boolean`- очищает корзину.


### Класс BuyerData
**Назначение и зона ответствености:** отвечает за данные покупателся и способ оплаты, а также за валидацую этих данных перед отправкой.


**Конструктор:**
`constructor()`- не принимает параметров, инициализирует пустые значения для данных покупателя.

**Поля класса:**
`payment:TPayment`- способ оплаты.
`address:string` - адрес доставки.
`phone:string` - номер телефона.
`email:string` - электорнная почта.

**Методы класса:**
`setBuyerData(data:Partial<IBuyer>):void`- принимает объект с данными покупателя.
`getBuyerData():IBuyer`- возвращает объект с текущими данными покупателя.
`validateBuyer():FormErrors`- проверяет валидацию полей.
`clear():void` - очищает данные покупателя после оформления заказа.





### Класс ProductData
**Назначение и зона ответстености:** отвечает за храние списка всех доступных товаров в магазине, и управление карточкой товара, которую пользователь выбрал.

**Конструктор:**
`constructor()` — не принимает параметров, при создании объекта инициализирует пустой массив товаров и устанавливает значение null для выбранной карточки.

**Поля класса:**
`products:IProduct[]`- массив товаров, загруженых с сервера.
`preview:IProduct|null`- товар для просомтра в модальном окне, или null если не выбран.

**Методы класса:**
`set products(value: IProduct[])` — сохраняет массив товаров в модель.
`get products(): IProduct[]` — возвращает массив всех товаров.
`get preview(): IProduct | null` — возвращает товар, выбранный для подробного просмотра.
`set preview(product: IProduct): void` — сохраняет выбранный товар в поле preview.
`getProduct(id: string): IProduct | undefined` — возвращает товар по идентификатору.



### Слой комуникации
Назначение и зона ответствености: Обеспечивает взаимодейстия приложения, с удаленым сервером путем выполнения    HTTP-запросов.Отвечает за получение данных, и отправку данных.


**Конструктор:**
`constructor(cdn: string, baseApi: IApi)`- сохраняет адрес сервера с картинками (cdn) и инструмент для запросов (baseApi) внутри класса.

**Поля класса:**
`cdn: string` — базовый адрес для картинок.
`baseApi: IApi` — инструмент для запросов.

**Методы класса:**
`getProductList(): Promise<IProduct[]>` — получает с сервера список всех товаров.
`postOrder(order: IOrder): Promise<IOrderResult>` — отправляет данные заказа на сервер и получает результат выполнения заказа.









## Витрина (View)

**Слой представления:** отвечает за отображение интерфейса приложения на странице и взаимодействие с пользователем. Классы этого слоя работают напрямую с DOM-разметкой и генерируют события для Презентера при действиях пользователя.


### Интерфейсы корзины:

**Окно корзины:** показывает список добавленных товаров и их итоговая стоимость.

```typescript
interface IBasketView{
items:HTMLElement[];
total:number;
}
```

**Карточки в корзине:** показывет номер товара, его цену, и порядковый номер.

```typescript
interface ICardBasketView{
title:string;
price:number|null;
index:number;
}
```

### Интерфейсы карточки

**Каталог карточек:** показыват список товаров на главной.

```typescript
interface ICardCatalogView{
title:string;
price:number;
image:string;
category:string;
}
```

**Выбранная карточка:** показыват всю информацию об выбраном товаре.

```typescript
interface  ICardPreview extends ICardCatalogView{
description:string;
}
```

### Интерфейсы формы

**Форма заказа:** предлагает выбор оплаты, и адресс доставки.

```typescript
interface IOrderFormView{
payment:'card'|'cash';
address:string;
errors:string;
}
```

**Форма контакта:** передать контактную информацию.

```typescript
interface IContactFormView{
email:string;
phone:string;
errors:string;
}
```


---
### Абстрактный класс Component
**Назначение:** является базовым классом для все компонентов слоя `View`.Содержит в себе общую логику работы с DOM-элементами, избавляя дочерние классы от дублирования. Класс является `дженериком` принимает тип данных `<Т>`, которые передает в методы.

**Поля класса**

`protected container:HTMLElement`-коренвой DOM-элемент компонента, внутри которого ведется рабоата и поиск дочерних  элеметов.

**Конструктор**

` protected constructor(container: HTMLElement)` — принимает корневой DOM-элемент компонента и сохраняет его в защищённое поле container.

**Методы класса**

`protected setImage(element: HTMLImageElement, src: string, alt?: string): void`- устанавливает путь к изображению и альтернативный текст для переданого HTML-элемента картинки.

`public render(data?: Partial<T>): HTMLElement`-принимает объект  который соответсвует типу Т, обновляет свойства компонента через их сеттеры и возвращает готовый DOM-элемент для отрисовки на странице.


---
### Абстрактный класс Card
**Назначение:** собирает в себе общую разметку дочерних элементов.

**Поля класса**

`protected events: IEvents` — брокер событий приложения для отправки сигналов Презентеру при действиях пользователя.
`protected titleElement:HTMLElement`- заголовок/название товар.
`protected priceElement:HTMLElement`- стоимость товара.

**Конструктор**

`pubkic constructor(container:HTMLElement,events:IEvents)`-принимает DOM-элемент карточки и экземляр бокера событий.Находит к контейнере элементы  названия и цены, сохраняет их в защищеные поля и рабоате с событиями.

**Методы класса**

`public set title(value:string):void`-записывает текстовое содержимоев в **titleElement**.
`public set price(value:number|null):void`-записывает значение цены в `priceElement`.Если цена равна null, выводит `Бесценно`.


---
### Промежуточный класс CardWithCategory
**Назначение:** промежуточный родитель для карточек которые помимо названия и цены содержат визуал.

**Поля класса**

`protected image:HTMLElement`-для отображения картинки товара.
`protected category:HTMLElement`-для отображения категории товара.

**Конструктор**

`public constructor(container:HTMLElement,event:Ievents)`- принимет контейнер разметки и брокер событий.

**Методы класса**

`public set image(value:string):void`-устанавливает адрес изображения  в атрибут `src` элемента `image`.
`public set category(value:string):устанвливает текст категории в элемент category.


---
### Класс CardCatalog
**Назначение**:наследует свойства от `CardWithCategory`.Используется для отображения краткой информации о товаре на главной витрине сайта.

**Конструктор**

`public constructor(container:HTMLElement,evemts:IEvents)`-принимает контейнер разметки и брокер событий, и передает их родителю для инициали цены, картинки, и категории.Вешает слушаетль клика на карточку для генерации события и открытия `CardPrewiev`.


---
### Класс CardPrewiev
**Назначение**: наледуется от `CardWithCategory`, используется для отображения товара в модальном окнем  с добавлением описания.

**Поля класса**

`private description:HTMLElement`-DOM элемент для вывода текстового описания товара.

**Конструтор**

`public constructo(container:HTMLElement,events:IEvents)`- находит внутри жлемнеты описания.Вешает кнопку покупки для отправки события и добалвения в корзину.

**Методы класса**

`public set description(value:string):void`- установливате описания товара в `description`.


---
### Класс BasketCard
**Назначение**:наследует напрямую от родителя Cards.Используется отображения товара в виде строки внутри корзины.Не содержит картинки и категории, но имеет порядковый номер.

**Поля класса**

`private indexElement:HTMLElement`-для отображения порядкового номера в списке в корзине.

**Конструктор**

`pulic constructor(container:HTMLElement, events:IEvents)`-принимает контейнер и брокер событий.Находит элемент индекса `indexElement` и `кнопку удаления товара`, на которую вешает генерацию события «удалить из корзины» для Презентера.

**Методы класса**

`public set index(value:number)`:void-устанавливает порядковый номер товара в `indexElement`.



---
### абстрактный класс Form
**Назначение**: отвечает за вывод ошибок, управление состоянием кнопки отправки активна/заблокирована и сбор данных из полей ввода.

**Поля класса

`protected submit:HTMLButtonElement`- кнопка отправки формы.
`protected errors:HTMLElement`-вывод ошибок, вылидация под формой.

**Конструктор**

`public constructor(container:HTMLElement, events:IEvents)`-принимат DOM жлемент формы и брокер событий.Находит кнопку отправки `submit` и элемент ошибок `errors` . Вешает слушателя событий на ввод и кнопку отправку формы.

**Методы класса**

`public set buttonAction(onClick:()=>|null)`-устанваливает обработчик событий на кнопку отправки.
`public set errors(valuer:string)`-принимает строку с ошибкой и отображает текст в `errors`.
`public set valid(value:boolean)`-упрвляет доступностью кнопки `submit`.Если false блокирует если true доступна.



---
### Класс OrderForm
**Назначение**:выбор способа оплаты онлайн/наличными и ввод адреса доставки.

**Поля класса**

`private payment:HTMLButtonElement[]`-DOM элементы для кнопок оплаты.
`private address:HTMLInputElement`- поля ввода адреса доставки.

**Конструктор**

`public constructor(container:HTMLElment,events:IEvents)`-вызывает родительский конструктор, находит в контейнере кнопки оплаты и инпут адреса. Вешает на них слушатели для фиксации выбора пользователя.

**Методы Класса**

`public set payment(value:cahs|card):void`-подсвечивает выбраную кнопку класса.
`public set address(value:string)`-устанавливает переданое текстовое значение в поле ввода `address`.


---
### Класс ContactsForm
**Значение**:отвечает конкретно за ввод почты и телефона.

**Поля класса**

`private email:HTMLInputElement`-поле email.
`private phone:HTMLINputElement`-поле phone.

**Конструктор**

`public constructor(container:HTMLElement,events:Ievents)`-вызывает родительский конструктор, находит инпуты почты и телефона, привязывая их к обработчикам событий ввода.

**Методы класса**

`public set email(value:string):void`- вставляет переданый текст в инпут `email`
`public set phone(value:string):void`-вставляет переданный текст в инпут `phone`.



---
### Класс Header
**Назначение**:отвечает за отображение шапки сайта.Управляет кнопкой корзины, и выводом количествао добавленных в нее товаров.

**Поля классов**

`private image:HTMLImageElement`-логотив в шапке сайта.
`private button:HTMLEButtonElement`-кнопка корзины.
`private counter:HTMLElement`-счетчик корзины.

**Конструктор**

`public constructor(conteiner:HTMLElement,events:IEvents)`-принимает корневой контейнер шапки и брокер событий.Находит кнопку корзины и элемент счетчика, а также вешает слушатель клика на кнопку корзины для вызова события открытия корзины в Презентере.

**Методы класса**

`public set counter(valuer:number):void`- принимает число и записывает в элемент counter, обновляет количество товаро в корзине.



---
### Класс Basket
**Значение**:отвечает за отображение корзины покупок, списка товаров, суммы и кнопки оформления.

**Поля классов**

` private title:HTMLElement`-заголовок корзины.
`private list:HTMLELement`- контейнер куда будут вставляться списки.
`private total:HTMLElement`-итоговая сумма.
`private button:HTMLBUuttonElement`-кнопка оформить.

**Конструктор**

`public constructor(container:HTMLElement,events:IEvents)`-находит элементы заголовка, списка товаров, общей стоимости и кнопки оформления.Вешает слушатель клика на кнопку «Оформить», который генерирует событие старта оформления заказа для Презентера.

**Поля класса**

`public set list(items:HTMLElement[]):void`-принимает массив DOM элементов карточек `BasketCard[]` и заменяет содержимое элемента `list`.
`public set toata(pirce:number):void`-обновляет текст в элементе total  выводя итоговую стоимость товаров.
`public set locke(value:boolean):void`-управляет доступностью кнопки, если корзина пуста блокирует кнопку чтобы пользователь не мог продолжить оформление без товаро.



---
### Класс Success
**Назначение**:отображает окно об успешно совершеной покупке.

**Поля класса**

`private close:HTMLElement`-кнопка закрытие окна.
`private total:HTMLElement`-вывод информации об списанной валюте.

**Конструктор**

`public constructor(container:HTMLElement,action:ISuccessActions)`-Находит в контейнере кнопку закрытия (close), элемент для вывода суммы (total) и вешает слушатель клика на кнопку для вызова коллбэка закрытия окна .

**Методы класса**

`public set total(value:number):void`-принимает числовое значение  и выводит текст с количеством списаных синопосв.








